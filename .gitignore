Nothing should be ignored based on the provided file changes. The changes only include HTML files (principiante.html, index.html) which are source files and not build artifacts, dependencies, or temporary files.